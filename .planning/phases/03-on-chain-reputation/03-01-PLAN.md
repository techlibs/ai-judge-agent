---
phase: 03-on-chain-reputation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/chain/reputation.ts
  - src/lib/chain/reputation-schemas.ts
  - src/app/api/reputation/[tokenId]/route.ts
  - src/components/reputation/reputation-summary-card.tsx
  - src/components/reputation/reputation-history-list.tsx
  - src/components/reputation/reputation-history-entry.tsx
  - src/components/reputation/transaction-link.tsx
  - src/components/reputation/on-chain-status-badge.tsx
  - src/app/proposals/[id]/reputation/page.tsx
autonomous: false
requirements:
  - CHAIN-05

must_haves:
  truths:
    - "User can view the full reputation history for any project showing all past evaluations with scores and timestamps"
    - "Each displayed score is independently verifiable on Base Sepolia block explorer via the provided transaction link"
    - "Reputation summary shows average score and total evaluation count"
    - "Empty states display correctly when no evaluations or no on-chain registration exist"
    - "Transaction links open the correct Base Sepolia block explorer page for the actual on-chain transaction"
    - "Page loads within reasonable time due to parallel RPC calls (not sequential)"
  artifacts:
    - path: "src/lib/chain/reputation.ts"
      provides: "On-chain reputation reading functions using viem with parallel RPC calls"
      exports: ["getReputationHistory", "getReputationSummary", "getTxHashesForBlocks"]
      contains: "Promise.all"
    - path: "src/lib/chain/reputation-schemas.ts"
      provides: "Zod schemas for on-chain reputation data at API boundary"
      exports: ["reputationFeedbackEntrySchema", "reputationSummarySchema", "reputationResponseSchema"]
      contains: "z.object"
    - path: "src/app/api/reputation/[tokenId]/route.ts"
      provides: "GET endpoint returning reputation history for a project by tokenId"
      exports: ["GET"]
    - path: "src/app/proposals/[id]/reputation/page.tsx"
      provides: "Reputation history page calling chain reader directly (no self-fetch)"
      contains: "getReputationHistory"
    - path: "src/components/reputation/reputation-summary-card.tsx"
      provides: "Summary card showing average score and evaluation count"
      contains: "ReputationSummaryCard"
    - path: "src/components/reputation/reputation-history-list.tsx"
      provides: "Chronological list of all evaluations with on-chain data"
      contains: "ReputationHistoryList"
    - path: "src/components/reputation/transaction-link.tsx"
      provides: "Truncated tx hash with copy and external link"
      contains: "TransactionLink"
    - path: "src/components/reputation/on-chain-status-badge.tsx"
      provides: "Status badge without as const assertions"
      contains: "satisfies"
  key_links:
    - from: "src/app/proposals/[id]/reputation/page.tsx"
      to: "src/lib/chain/reputation.ts"
      via: "direct import of getReputationHistory, getReputationSummary, getTxHashesForBlocks"
      pattern: "import.*getReputationHistory.*from.*reputation"
    - from: "src/app/api/reputation/[tokenId]/route.ts"
      to: "src/lib/chain/reputation.ts"
      via: "import getReputationHistory, getReputationSummary, getTxHashesForBlocks"
      pattern: "getReputationHistory|getReputationSummary|getTxHashesForBlocks"
    - from: "src/app/api/reputation/[tokenId]/route.ts"
      to: "src/lib/chain/reputation-schemas.ts"
      via: "import reputationResponseSchema for Zod validation"
      pattern: "reputationResponseSchema"
    - from: "src/components/reputation/transaction-link.tsx"
      to: "https://sepolia.basescan.org/tx/"
      via: "external link href using real txHash from event logs"
      pattern: "sepolia\\.basescan\\.org/tx"

user_setup:
  - service: blockchain
    why: "Reading on-chain reputation data from Base Sepolia"
    env_vars:
      - name: NEXT_PUBLIC_RPC_URL
        source: "Base Sepolia RPC URL -- should already exist from Phase 1/2 (e.g., https://sepolia.base.org)"
      - name: NEXT_PUBLIC_REPUTATION_REGISTRY_ADDRESS
        source: "ERC-8004 ReputationRegistry address -- should already exist from Phase 1 (0x8004B663056A597Dffe9eCcC1965A193B7388713)"
      - name: NEXT_PUBLIC_IDENTITY_REGISTRY_ADDRESS
        source: "ERC-8004 IdentityRegistry address -- should already exist from Phase 1 (0x8004A818BFB912233c491871b3d84c89A494BD9e)"
---

<objective>
Build the reputation history querying system: a viem-based chain reader that recovers real transaction hashes from event logs using parallel RPC calls, a Zod-validated Next.js API route for external consumers, and the complete set of UI components showing per-project reputation history with verified on-chain transaction links. The server component page calls chain reader functions directly (no self-fetch anti-pattern).

Purpose: CHAIN-05 requires reputation history per project to be queryable from on-chain events. This plan creates the read path -- calling ReputationRegistry.readAllFeedback() and getSummary() via viem, recovering txHashes via getLogs on the FeedbackGiven event with parallelized RPC calls -- and renders results in a UI that links every evaluation to its actual on-chain transaction for independent verification.

Output: Working reputation history page accessible at /proposals/{id}/reputation, showing summary stats and chronological evaluation history with real block explorer links to verified transactions. The page server component calls chain reader functions directly with Next.js unstable_cache for caching. A separate API route exists for external/programmatic consumers.

Key review feedback addressed:
- HIGH: Parallel RPC calls via Promise.all in getTxHashesForBlocks and getReputationHistory
- MEDIUM: Server component calls chain reader directly instead of self-fetching API route
- MEDIUM: Explicit tokenId-proposalId mapping documented and handled
- MEDIUM: No `as const` assertions; uses `satisfies` pattern instead
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-on-chain-reputation/03-RESEARCH.md
@.planning/phases/03-on-chain-reputation/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts from Phase 1 and Phase 2 that this plan depends on -->

From src/lib/chain/client.ts (Phase 1, Plan 03):
```typescript
// Viem public client for Base Sepolia -- use for readContract calls
import { createPublicClient, http } from "viem";
import { baseSepolia } from "viem/chains";

export const publicClient = createPublicClient({
  chain: baseSepolia,
  transport: http(process.env.NEXT_PUBLIC_RPC_URL),
});
```

From src/lib/chain/contracts.ts (Phase 1, Plan 03):
```typescript
export const IDENTITY_REGISTRY_ADDRESS = process.env.NEXT_PUBLIC_IDENTITY_REGISTRY_ADDRESS as `0x${string}`;
export const REPUTATION_REGISTRY_ADDRESS = process.env.NEXT_PUBLIC_REPUTATION_REGISTRY_ADDRESS as `0x${string}`;
// Contains ABI imports for both registries
```

From src/lib/evaluation/schemas.ts (Phase 2, Plan 01):
```typescript
export type EvaluationDimension = "technical" | "impact" | "cost" | "team";
export type ProposalEvaluation = {
  proposalId: string;
  dimensions: DimensionEvaluation[];
  aggregate: { weightedScore: number; completedDimensions: number; computedAt: number };
  status: "evaluating" | "evaluated" | "failed";
};
```

ERC-8004 ReputationRegistry key functions (from RESEARCH.md):
```solidity
// getSummary returns: (count, summaryValue, summaryValueDecimals)
function getSummary(uint256 agentId, address[] clients, string tag1, string tag2) view returns (uint256, int128, uint8);

// readAllFeedback returns array of feedback structs
function readAllFeedback(uint256 agentId) view returns (Feedback[] memory);

// Feedback struct contains: clientAddress, value, valueDecimals, tag1, tag2, endpoint, feedbackURI, feedbackHash, blockNumber
```

ERC-8004 ReputationRegistry events (from Phase 1, Plan 01):
```solidity
// Emitted by giveFeedback -- used to recover txHash in Phase 3
event FeedbackGiven(uint256 indexed tokenId, address indexed evaluator, uint256 score, string contentHash)
```

**tokenId / proposalId mapping (addresses review concern):**
In this system, the on-chain tokenId (ERC-8004 agentId) is assigned when the project is first registered via IdentityRegistry.register() in Phase 1. The proposal detail page URL uses the proposal's internal ID (e.g., IPFS CID or sequential ID). The mapping from proposalId to on-chain tokenId is stored in the proposal's on-chain registration data. The reputation page route uses `[id]` which is the proposalId -- the page must resolve this to the on-chain tokenId before querying the ReputationRegistry. Phase 1 Plan 03's on-chain registration stores a mapping that Phase 3 reads. If no mapping exists (project not registered), the page shows the "Not yet on-chain" empty state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reputation chain reader with parallel RPC calls, Zod schemas, and API route</name>
  <files>src/lib/chain/reputation-schemas.ts, src/lib/chain/reputation.ts, src/app/api/reputation/[tokenId]/route.ts</files>
  <read_first>
    src/lib/chain/client.ts
    src/lib/chain/contracts.ts
    src/lib/evaluation/schemas.ts
    .planning/phases/03-on-chain-reputation/03-RESEARCH.md
  </read_first>
  <action>
**Create `src/lib/chain/reputation-schemas.ts`:**

Define Zod schemas for reputation data at the API boundary, per CLAUDE.md "Zod validation at boundaries" rule. These schemas validate the data returned from viem readContract calls before it leaves the API route.

```typescript
import { z } from "zod";

export const reputationFeedbackEntrySchema = z.object({
  clientAddress: z.string(),
  value: z.number(),
  tag1: z.string(),
  tag2: z.string(),
  feedbackURI: z.string(),
  feedbackHash: z.string(),
  blockNumber: z.number().int().nonnegative(),
  timestamp: z.number().int().nonnegative(),
  txHash: z.string().regex(/^0x[a-fA-F0-9]{64}$/).nullable(),
});

export const reputationSummarySchema = z.object({
  feedbackCount: z.number().int().nonnegative(),
  averageScore: z.number().min(0).max(100),
});

export const reputationResponseSchema = z.object({
  tokenId: z.string(),
  summary: reputationSummarySchema,
  history: z.array(reputationFeedbackEntrySchema),
});

export type ReputationFeedbackEntry = z.infer<typeof reputationFeedbackEntrySchema>;
export type ReputationSummary = z.infer<typeof reputationSummarySchema>;
export type ReputationResponse = z.infer<typeof reputationResponseSchema>;
```

**Create `src/lib/chain/reputation.ts`:**

This file provides three functions that read reputation data from the on-chain ReputationRegistry using viem. **Addresses review concern HIGH: All RPC calls are parallelized using Promise.all/Promise.allSettled.**

Import the existing `publicClient` from `src/lib/chain/client.ts` and `REPUTATION_REGISTRY_ADDRESS` from `src/lib/chain/contracts.ts`. Import the ReputationRegistry ABI (same ABI already used in Phase 2 storage.ts for `giveFeedback` writes). Import types from `reputation-schemas.ts`.

**`getReputationHistory(agentId: string): Promise<Omit<ReputationFeedbackEntry, "txHash">[]>`**

1. Call `publicClient.readContract({ address: REPUTATION_REGISTRY_ADDRESS, abi: reputationRegistryAbi, functionName: "readAllFeedback", args: [BigInt(agentId)] })`.
2. The return is an array of feedback structs. Map each to a partial entry (without txHash -- that comes from getTxHashesForBlocks):
   - `value`: Convert from fixed-point. The contract stores `int128 value` with `uint8 valueDecimals`. Compute: `Number(feedback.value) / Math.pow(10, Number(feedback.valueDecimals))`.
   - `blockNumber`: `Number(feedback.blockNumber)`.
   - `feedbackHash`: Convert `bytes32` to hex string.
   - `feedbackURI`, `tag1`, `tag2`, `clientAddress`: direct string values.
3. **PARALLEL block timestamp fetching (addresses review HIGH concern):** Collect all unique block numbers. Fetch ALL block timestamps in parallel using Promise.all:
   ```typescript
   const uniqueBlocks = [...new Set(entries.map(e => e.blockNumber))];
   const blockResults = await Promise.all(
     uniqueBlocks.map(blockNum =>
       publicClient.getBlock({ blockNumber: BigInt(blockNum) })
     )
   );
   const timestampMap = new Map<number, number>();
   uniqueBlocks.forEach((blockNum, i) => {
     timestampMap.set(blockNum, Number(blockResults[i].timestamp));
   });
   ```
   Then assign timestamps: `entry.timestamp = timestampMap.get(entry.blockNumber) ?? 0;`
4. Sort entries by blockNumber descending (newest first).
5. Return the array.
6. Wrap the entire function in try/catch. On error, log the error and return an empty array (graceful degradation -- the UI shows an empty state).

**`getReputationSummary(agentId: string): Promise<ReputationSummary>`**

1. Call `publicClient.readContract({ address: REPUTATION_REGISTRY_ADDRESS, abi: reputationRegistryAbi, functionName: "getSummary", args: [BigInt(agentId), [], "grant-evaluation", ""] })`.
   - First arg: agentId as BigInt.
   - Second arg: empty array for clientAddresses (query all).
   - Third arg: `"grant-evaluation"` (tag1 used in Phase 2 publishScoreOnChain).
   - Fourth arg: `""` (tag2 empty for any version).
2. The return is `[count: bigint, summaryValue: bigint, summaryValueDecimals: number]`.
3. Convert: `feedbackCount = Number(count)`, `averageScore = Number(summaryValue) / Math.pow(10, Number(summaryValueDecimals))`.
4. Return `{ feedbackCount, averageScore }`.
5. On error, return `{ feedbackCount: 0, averageScore: 0 }`.

**`getTxHashesForBlocks(agentId: string, blockNumbers: number[]): Promise<Map<number, string>>`**

This function recovers the actual transaction hashes from on-chain event logs. Phase 2's `publishScoreOnChain` calls `giveFeedback` which emits a `FeedbackGiven` event. We query these events by block number to get the real txHash.

**Addresses review concern HIGH: All getLogs calls run in parallel via Promise.allSettled.**

1. Deduplicate blockNumbers: `const uniqueBlocks = [...new Set(blockNumbers)]`.
2. Import `parseAbiItem` from viem. Define the event signature: `const feedbackGivenEvent = parseAbiItem("event FeedbackGiven(uint256 indexed tokenId, address indexed evaluator, uint256 score, string contentHash)")`.
3. **PARALLEL execution:** Fire all getLogs calls simultaneously using Promise.allSettled (not Promise.all, so one failure doesn't abort the rest):
   ```typescript
   const results = await Promise.allSettled(
     uniqueBlocks.map(blockNum =>
       publicClient.getLogs({
         address: REPUTATION_REGISTRY_ADDRESS,
         event: feedbackGivenEvent,
         args: { tokenId: BigInt(agentId) },
         fromBlock: BigInt(blockNum),
         toBlock: BigInt(blockNum),
       })
     )
   );
   ```
4. Build the Map from settled results:
   ```typescript
   const txMap = new Map<number, string>();
   results.forEach((result, i) => {
     if (result.status === "fulfilled" && result.value.length > 0) {
       txMap.set(uniqueBlocks[i], result.value[0].transactionHash);
     }
   });
   ```
5. Return the map. Failed individual lookups produce null txHash in the response (graceful per-entry degradation).

**Create `src/app/api/reputation/[tokenId]/route.ts`:**

This API route exists for external/programmatic consumers. The server component page calls chain reader functions directly (see Task 2).

```typescript
import { type NextRequest, NextResponse } from "next/server";
import { getReputationHistory, getReputationSummary, getTxHashesForBlocks } from "@/lib/chain/reputation";
import { reputationResponseSchema } from "@/lib/chain/reputation-schemas";
```

**`export async function GET(request: NextRequest, { params }: { params: Promise<{ tokenId: string }> }): Promise<NextResponse>`**

1. Extract `tokenId` from `await params`. Validate it is a non-empty string that can be parsed as a positive integer: `const id = (await params).tokenId; if (!/^\d+$/.test(id)) return NextResponse.json({ error: "Invalid tokenId" }, { status: 400 });` (Addresses T-03-01 threat).
2. Call history and summary in parallel: `const [history, summary] = await Promise.all([getReputationHistory(id), getReputationSummary(id)]);`
3. Recover txHashes for all blocks in the history: `const blockNumbers = history.map(e => e.blockNumber); const txHashMap = await getTxHashesForBlocks(id, blockNumbers);`
4. Enrich history entries with txHashes: `const enrichedHistory = history.map(entry => ({ ...entry, txHash: txHashMap.get(entry.blockNumber) ?? null }));`
5. Build response object: `const responseData = { tokenId: id, summary, history: enrichedHistory };`
6. Validate through Zod before returning: `const validated = reputationResponseSchema.parse(responseData);`
7. Create response: `const response = NextResponse.json(validated);`
8. Add Cache-Control header: `response.headers.set("Cache-Control", "public, s-maxage=30, stale-while-revalidate=60");`
9. Return response.
10. On error: `NextResponse.json({ error: "Failed to fetch reputation data" }, { status: 500 })`.
  </action>
  <verify>
    <automated>cd /Users/libardo/carlos/projects/ipe-city/agent-reviewer/.worktrees/full-vision && bun run typecheck 2>&1 | grep -E "(error|src/lib/chain/reputation|src/app/api/reputation)" ; echo "---" ; grep -c "Promise.all\|Promise.allSettled" src/lib/chain/reputation.ts ; grep -c "readContract" src/lib/chain/reputation.ts ; grep -c "getLogs" src/lib/chain/reputation.ts ; grep -c "z.object" src/lib/chain/reputation-schemas.ts ; grep -c "GET" src/app/api/reputation/\[tokenId\]/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/chain/reputation-schemas.ts exists and exports Zod schemas: `reputationFeedbackEntrySchema`, `reputationSummarySchema`, `reputationResponseSchema`
    - src/lib/chain/reputation-schemas.ts exports types: `ReputationFeedbackEntry`, `ReputationSummary`, `ReputationResponse`
    - src/lib/chain/reputation-schemas.ts contains `txHash: z.string().regex` with nullable for null txHashes
    - src/lib/chain/reputation.ts exists and exports `getReputationHistory`, `getReputationSummary`, `getTxHashesForBlocks`
    - src/lib/chain/reputation.ts contains `readContract` calls (at least 2 occurrences)
    - src/lib/chain/reputation.ts contains `getLogs` call for FeedbackGiven event recovery
    - src/lib/chain/reputation.ts contains `parseAbiItem` for event definition
    - src/lib/chain/reputation.ts contains `BigInt(agentId)` for BigInt conversion
    - src/lib/chain/reputation.ts contains `readAllFeedback` and `getSummary` function name strings
    - src/lib/chain/reputation.ts contains `grant-evaluation` tag string
    - src/lib/chain/reputation.ts does NOT contain `any` type
    - **[Review HIGH] src/lib/chain/reputation.ts contains `Promise.all` for parallel getBlock calls in getReputationHistory (grep: "Promise.all" appears at least once)**
    - **[Review HIGH] src/lib/chain/reputation.ts contains `Promise.allSettled` for parallel getLogs calls in getTxHashesForBlocks (grep: "Promise.allSettled" appears at least once)**
    - src/app/api/reputation/[tokenId]/route.ts exports GET function
    - src/app/api/reputation/[tokenId]/route.ts contains `reputationResponseSchema.parse` for Zod validation at boundary
    - src/app/api/reputation/[tokenId]/route.ts contains validation: `/^\d+$/.test` or equivalent regex check
    - src/app/api/reputation/[tokenId]/route.ts contains `Promise.all` for parallel history+summary fetch
    - src/app/api/reputation/[tokenId]/route.ts contains `Cache-Control` header
    - src/app/api/reputation/[tokenId]/route.ts returns 400 for invalid tokenId
    - bun run typecheck passes with no errors in reputation files
  </acceptance_criteria>
  <done>
    GET /api/reputation/{tokenId} returns Zod-validated JSON with summary (feedbackCount, averageScore) and history (array of entries with value, blockNumber, timestamp, feedbackURI, feedbackHash, and real txHash recovered from FeedbackGiven event logs). All RPC calls within getReputationHistory (getBlock) and getTxHashesForBlocks (getLogs) execute in parallel via Promise.all/Promise.allSettled. Invalid tokenId returns 400. Types are strict (no `any`). Response includes Cache-Control header for CDN/edge caching.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn table/tooltip components and build reputation UI with direct chain reader calls</name>
  <files>src/components/reputation/reputation-summary-card.tsx, src/components/reputation/reputation-history-list.tsx, src/components/reputation/reputation-history-entry.tsx, src/components/reputation/transaction-link.tsx, src/components/reputation/on-chain-status-badge.tsx, src/app/proposals/[id]/reputation/page.tsx</files>
  <read_first>
    .planning/phases/03-on-chain-reputation/03-UI-SPEC.md
    src/lib/chain/reputation-schemas.ts
    src/lib/chain/reputation.ts
    src/app/api/reputation/[tokenId]/route.ts
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
    src/app/proposals/[id]/page.tsx
  </read_first>
  <action>
**Step 0: Install required shadcn components**

Run: `bunx shadcn@latest add table tooltip`

These are required by the UI-SPEC and may not exist from prior phases.

**Create `src/components/reputation/on-chain-status-badge.tsx`:**

Server component (no `"use client"`). Accepts `status: "confirmed" | "pending" | "failed"`. Uses shadcn Badge component.

**Addresses review concern MEDIUM: No `as const` assertions. Use `satisfies` pattern instead per CLAUDE.md "zero tolerance for type escapes" rule.**

```typescript
import { Badge, type BadgeProps } from "@/components/ui/badge";

type OnChainStatus = "confirmed" | "pending" | "failed";

type StatusConfig = {
  label: string;
  variant: BadgeProps["variant"];
  className: string;
};

const STATUS_CONFIG: Record<OnChainStatus, StatusConfig> = {
  confirmed: { label: "Confirmed", variant: "default", className: "bg-chart-1 text-chart-1-foreground" },
  pending: { label: "Pending", variant: "secondary", className: "text-muted-foreground" },
  failed: { label: "Failed", variant: "destructive", className: "" },
};
```

Note: The `variant` values ("default", "secondary", "destructive") are typed via `BadgeProps["variant"]` -- no `as const` needed. The `STATUS_CONFIG` object is typed with `Record<OnChainStatus, StatusConfig>` which constrains both keys and values without type assertions.

Render: `<Badge variant={config.variant} className={config.className} aria-label={`On-chain status: ${config.label}`}>{config.label}</Badge>`

**Create `src/components/reputation/transaction-link.tsx`:**

Client component (`"use client"`). Accepts `txHash: string | null`. Uses Lucide icons `Copy`, `Check`, `ExternalLink`. Uses shadcn Tooltip.

- If `txHash` is null, render a muted "Pending" text with no link (graceful handling when txHash could not be recovered from event logs).
- Display truncated hash: `{txHash.slice(0, 6)}...{txHash.slice(-4)}` in `font-mono text-sm` span.
- Wrap hash in TooltipProvider > Tooltip > TooltipTrigger/TooltipContent showing the full hash.
- Copy button: `onClick` copies `txHash` to clipboard via `navigator.clipboard.writeText(txHash)`. Uses `useState` for `copied` boolean. On click set `copied = true`, `setTimeout(() => setCopied(false), 2000)`. Show `Check` icon when copied, `Copy` icon otherwise. Size: 16px (w-4 h-4). `aria-label="Copy transaction hash to clipboard"` / `"Transaction hash copied"`.
- External link: `<a href={`https://sepolia.basescan.org/tx/${txHash}`} target="_blank" rel="noopener noreferrer" aria-label={`View transaction ${txHash.slice(0, 6)}...${txHash.slice(-4)} on Base Sepolia block explorer`}>` with ExternalLink icon (w-4 h-4, `text-muted-foreground hover:text-chart-1` classes).

**IMPORTANT:** The `txHash` passed to this component is a real transaction hash recovered from on-chain FeedbackGiven event logs (see Task 1, getTxHashesForBlocks). It is NOT the feedbackHash (content hash). The block explorer link `sepolia.basescan.org/tx/{txHash}` will correctly resolve to the giveFeedback transaction.

**Create `src/components/reputation/reputation-summary-card.tsx`:**

Server component. Props: `summary: { feedbackCount: number; averageScore: number }; agentId: string`.

Uses shadcn Card, CardHeader, CardTitle, CardContent. Per UI-SPEC:

- CardTitle: "On-Chain Reputation"
- Content layout:
  - Desktop (md+): horizontal row with 3 sections separated by dividers
  - Mobile: vertical stack
- Average score: `<span className="text-[28px] font-semibold leading-[1.2] text-chart-1">{summary.averageScore.toFixed(1)}/100</span>` with label "Average Score" above in `text-sm text-muted-foreground`.
  - `aria-label={`Score: ${summary.averageScore.toFixed(1)} out of 100`}`
- Evaluation count: `<span className="text-sm">Based on {summary.feedbackCount} evaluation{summary.feedbackCount !== 1 ? "s" : ""}</span>`
- Agent ID: Wrapped in TooltipProvider > Tooltip. Trigger text: `Identity #{agentId}`. Tooltip content: `ERC-8004 Identity Token #{agentId} on Base Sepolia`. Text: `text-sm text-muted-foreground`.

**Create `src/components/reputation/reputation-history-entry.tsx`:**

Client component (`"use client"`). Props:

```typescript
import { type ReputationFeedbackEntry } from "@/lib/chain/reputation-schemas";

type ReputationHistoryEntryProps = {
  entry: ReputationFeedbackEntry;
};
```

Mobile layout (default): Vertical card with score, relative timestamp, TransactionLink, block number, OnChainStatusBadge, "View on chain" link.

Desktop layout (md+): Table row via shadcn TableRow > TableCell elements.

- Score: `<span className="text-base font-semibold">{entry.value.toFixed(0)}/100</span>` with `aria-label={`Score: ${entry.value} out of 100`}`
- Timestamp: Use `Intl.RelativeTimeFormat` or a helper to compute relative time from `entry.timestamp * 1000` (Unix seconds to ms). Show relative string (e.g., "3 days ago"). Add `title={new Date(entry.timestamp * 1000).toISOString()}` for full ISO on hover.
- Block number: `<span className="text-sm font-mono">#{entry.blockNumber}</span>`
- TransactionLink: `<TransactionLink txHash={entry.txHash} />` -- passes the real txHash (or null if unavailable)
- Status: `<OnChainStatusBadge status="confirmed" />` (if entry exists on-chain, it is confirmed)
- Row is clickable: wraps in `<Link href={`/proposals/${proposalId}/evaluation`}>` (derive proposalId from feedbackURI if it contains the proposal URL, or pass as prop).

**Create `src/components/reputation/reputation-history-list.tsx`:**

Server component. Props: `history: ReputationFeedbackEntry[]; agentId: string`.

- If `history.length === 0` and agentId exists: Show empty state card per UI-SPEC: Lucide `History` icon (48px, `text-muted-foreground`), heading "No reputation history", body text "This project has not been evaluated yet. Reputation data will appear here after the first evaluation is published on-chain."
- If no agentId (project not registered): Show empty state with Lucide `LinkOff` icon (48px), heading "Not yet on-chain", body "This project has not been registered on the blockchain yet. On-chain identity is created when a proposal is first submitted."
- Section heading: `<h2 className="text-xl font-semibold leading-[1.2]">Reputation History</h2>`

Desktop (md+): Render as shadcn Table with columns: Date, Score, Tx Hash, Block, Status, Link. Use `<div className="hidden md:block">` wrapper.

Mobile (default): Render as `<div role="list">` with each entry as `<div role="listitem">` containing a card-style layout. Use `<div className="md:hidden">` wrapper.

Map over history entries, rendering `<ReputationHistoryEntry>` for each.

**Addresses review concern LOW (error boundary):** Wrap the history list in a React Error Boundary. Create a simple `ReputationErrorBoundary` client component at the top of `reputation-history-list.tsx` (or in a separate small file) that catches render errors and shows the UI-SPEC error state: Lucide `AlertTriangle` icon, heading "Could not load reputation data", body "We were unable to read on-chain reputation data. This may be a temporary network issue. Please try again later.", and a "Try again" button that calls `window.location.reload()`.

**Create `src/app/proposals/[id]/reputation/page.tsx`:**

**Addresses review concern MEDIUM: Server component calls chain reader functions DIRECTLY instead of self-fetching from the API route. Uses Next.js `unstable_cache` for caching.**

Server component page. This is the reputation history page for a specific project.

```typescript
import { unstable_cache } from "next/cache";
import { getReputationHistory, getReputationSummary, getTxHashesForBlocks } from "@/lib/chain/reputation";
import { ReputationSummaryCard } from "@/components/reputation/reputation-summary-card";
import { ReputationHistoryList } from "@/components/reputation/reputation-history-list";
import { reputationResponseSchema } from "@/lib/chain/reputation-schemas";
```

**Addresses review concern MEDIUM (tokenId mapping):** The `[id]` param is the proposalId from the URL. The page must resolve this to the on-chain tokenId. Read the existing proposal data (from Phase 1's proposal detail page pattern) to get the `tokenId` / `agentId`. If the proposal page already fetches proposal data that includes the on-chain tokenId, reuse that lookup. If the tokenId is the same as the proposalId (i.e., they share a namespace), use it directly. **Document the mapping in a code comment:** `// [id] is the proposalId. We resolve it to the on-chain tokenId via [lookup mechanism from Phase 1].` If no tokenId exists for this proposal (not registered on-chain), render the "Not yet on-chain" empty state.

1. Extract `id` from `params`.
2. Resolve `id` to on-chain `tokenId` (see mapping note above).
3. Create a cached fetcher using `unstable_cache`:
   ```typescript
   const getCachedReputationData = unstable_cache(
     async (tokenId: string) => {
       const [history, summary] = await Promise.all([
         getReputationHistory(tokenId),
         getReputationSummary(tokenId),
       ]);
       const blockNumbers = history.map(e => e.blockNumber);
       const txHashMap = await getTxHashesForBlocks(tokenId, blockNumbers);
       const enrichedHistory = history.map(entry => ({
         ...entry,
         txHash: txHashMap.get(entry.blockNumber) ?? null,
       }));
       return reputationResponseSchema.parse({
         tokenId,
         summary,
         history: enrichedHistory,
       });
     },
     ["reputation-data"],
     { revalidate: 30 }
   );
   ```
4. Call `const data = await getCachedReputationData(tokenId);`
5. Render page with `max-w-6xl mx-auto` container, `p-4 md:p-8` padding:
   - `<ReputationSummaryCard summary={data.summary} agentId={tokenId} />`
   - `<div className="mt-6 md:mt-8"><ReputationHistoryList history={data.history} agentId={tokenId} /></div>`
6. Add page metadata: `export const metadata = { title: "Reputation History" };`

**Error handling:** Wrap the data fetch in try/catch. On error, render the error state per UI-SPEC: Lucide `AlertTriangle` icon, heading "Could not load reputation data", body text, and "Try again" link (since this is a server component, use a `<Link href={currentPath}>` or `<a href="">` for refresh instead of a client-side button).
  </action>
  <verify>
    <automated>cd /Users/libardo/carlos/projects/ipe-city/agent-reviewer/.worktrees/full-vision && bun run typecheck 2>&1 | grep -E "(error|src/components/reputation|src/app/proposals)" ; echo "---" ; ls src/components/reputation/ | wc -l ; grep -l "use client" src/components/reputation/*.tsx 2>/dev/null ; echo "---" ; grep -c "unstable_cache\|getReputationHistory" src/app/proposals/\[id\]/reputation/page.tsx ; grep "as const" src/components/reputation/on-chain-status-badge.tsx || echo "NO_AS_CONST_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/reputation/ directory contains exactly 5 files: on-chain-status-badge.tsx, transaction-link.tsx, reputation-summary-card.tsx, reputation-history-entry.tsx, reputation-history-list.tsx
    - src/app/proposals/[id]/reputation/page.tsx exists
    - **[Review MEDIUM] src/app/proposals/[id]/reputation/page.tsx imports `getReputationHistory` directly (grep: "import.*getReputationHistory.*from.*reputation")**
    - **[Review MEDIUM] src/app/proposals/[id]/reputation/page.tsx contains `unstable_cache` for caching (grep: "unstable_cache")**
    - **[Review MEDIUM] src/app/proposals/[id]/reputation/page.tsx does NOT contain `fetch` calling its own API route (grep for `api/reputation` in page.tsx should return 0 matches)**
    - **[Review MEDIUM] src/app/proposals/[id]/reputation/page.tsx contains a comment explaining the tokenId/proposalId mapping**
    - transaction-link.tsx contains `"use client"` directive
    - transaction-link.tsx contains `sepolia.basescan.org/tx/` URL
    - transaction-link.tsx contains `navigator.clipboard.writeText`
    - transaction-link.tsx contains `aria-label` attributes for copy button and external link
    - transaction-link.tsx handles null txHash gracefully (renders muted "Pending" text)
    - reputation-summary-card.tsx contains text "On-Chain Reputation" (card title)
    - reputation-summary-card.tsx contains text "Average Score" label
    - reputation-summary-card.tsx contains `text-[28px]` or equivalent 28px size class for display score
    - reputation-history-list.tsx contains empty state heading "No reputation history"
    - reputation-history-list.tsx contains empty state heading "Not yet on-chain"
    - reputation-history-list.tsx contains `role="list"` for mobile layout
    - reputation-history-entry.tsx contains `font-mono` class for block number display
    - reputation-history-entry.tsx imports `ReputationFeedbackEntry` type from reputation-schemas
    - **[Review MEDIUM] on-chain-status-badge.tsx does NOT contain `as const` (grep: "as const" returns 0 matches)**
    - **[Review MEDIUM] on-chain-status-badge.tsx contains `Record<` or `satisfies` for type-safe config (grep: "Record<" or "satisfies")**
    - on-chain-status-badge.tsx contains all three statuses: "Confirmed", "Pending", "Failed"
    - on-chain-status-badge.tsx contains `aria-label` with "On-chain status:"
    - All tsx files in src/components/reputation/ do NOT contain the string `: any` or `as any`
    - src/components/ui/table.tsx exists (installed via shadcn)
    - src/components/ui/tooltip.tsx exists (installed via shadcn)
    - bun run typecheck passes with no errors in reputation component files
  </acceptance_criteria>
  <done>
    Reputation history page at /proposals/{id}/reputation calls chain reader functions directly via unstable_cache (no self-fetch anti-pattern). Renders summary card with average score and evaluation count, plus chronological history list with scores, timestamps, real transaction hashes (truncated with copy+explorer link), block numbers, and status badges. Block explorer links use actual txHashes recovered from FeedbackGiven event logs (not feedbackHash). on-chain-status-badge uses Record type instead of `as const` assertions. Empty states display for unregistered projects and projects with no evaluations. Error boundary catches unexpected render errors. Desktop shows table layout, mobile shows card layout. All accessibility attributes present per UI-SPEC.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of reputation history page</name>
  <files>none</files>
  <action>Human verifies the complete reputation history feature end-to-end.</action>
  <verify>Human approval</verify>
  <done>User confirms reputation page works correctly.</done>
  <what-built>Complete reputation history page with on-chain data reading via parallel RPC calls (Promise.all/Promise.allSettled), Zod-validated API route for external consumers, server component page calling chain reader directly via unstable_cache, summary card, history list, transaction links to Base Sepolia explorer using real txHashes from event logs, copy-to-clipboard, responsive layout (table on desktop, cards on mobile), error boundary, and empty states. No `as const` assertions anywhere.</what-built>
  <how-to-verify>
    1. Run the dev server: `bun run dev`
    2. Navigate to `/proposals/{id}/reputation` where `{id}` is an existing proposal with on-chain registration (e.g., from a proposal submitted in Phase 1/2)
    3. Verify the summary card shows "On-Chain Reputation" title with average score and evaluation count
    4. Verify history entries show score, relative timestamp, truncated tx hash, block number, and "Confirmed" badge
    5. **[Review HIGH validation]** Verify the page loads without noticeable delay (parallel RPC calls should make it fast). Check browser network tab -- there should be NO fetch to `/api/reputation/` from the page itself (the page calls chain reader directly).
    6. Click the copy icon next to a tx hash -- verify the hash is copied to clipboard and icon changes to checkmark for 2 seconds
    7. Click "View on chain" or the external link icon -- verify it opens sepolia.basescan.org/tx/{hash} in a new tab AND the transaction page loads correctly (not "transaction not found")
    8. Hover over the truncated tx hash -- verify tooltip shows the full hash
    9. Hover over the agent ID in the summary card -- verify tooltip shows "ERC-8004 Identity Token #{id} on Base Sepolia"
    10. Resize browser to mobile width (< 768px) -- verify history switches from table to stacked card layout
    11. Navigate to a proposal with no evaluations -- verify "No reputation history" empty state
    12. Navigate to a non-existent proposal (not registered on-chain) -- verify "Not yet on-chain" empty state or graceful handling (no crash)
    13. Also verify the API route works independently: `curl http://localhost:3000/api/reputation/1` should return JSON with summary and history fields
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API route | User-supplied tokenId in URL path parameter |
| API route -> Base Sepolia RPC | Server reads from public blockchain via RPC |
| API route -> Response | On-chain data validated through Zod before returning |
| UI -> Block explorer | External link to sepolia.basescan.org using real txHash |
| Page -> Chain reader | Server component calls chain functions directly (no HTTP boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | API route tokenId param | mitigate | Validate tokenId is numeric-only via regex `/^\d+$/` before passing to viem BigInt conversion; reject non-numeric with 400 |
| T-03-02 | Information Disclosure | RPC URL in client code | mitigate | Use NEXT_PUBLIC_RPC_URL env var; do not hardcode RPC endpoints or API keys in source |
| T-03-03 | Denial of Service | Excessive RPC calls | mitigate | Server component uses unstable_cache with revalidate=30; API route uses Cache-Control header (s-maxage=30, stale-while-revalidate=60); RPC calls parallelized to minimize wall-clock time per request |
| T-03-04 | Spoofing | Fake block explorer link | accept | We construct the URL deterministically from `sepolia.basescan.org/tx/{txHash}` where txHash is recovered from on-chain event logs -- no user-supplied URL injection. Low risk as basescan is the canonical explorer. |
| T-03-05 | Information Disclosure | Private key in reputation read | accept | Reputation reads use publicClient only (no wallet/private key needed). No secrets involved in read operations. |
| T-03-06 | Denial of Service | Large readAllFeedback result | accept | For v1, evaluation count per project will be small (< 100). UI-SPEC specifies "Load more" at 20 entries. Re-evaluate if production volume increases. |
| T-03-07 | Tampering | Unvalidated chain data | mitigate | All readContract return values parsed through Zod schemas (reputationResponseSchema.parse) before leaving the API route boundary and before rendering in page component |
</threat_model>

<verification>
1. `bun run typecheck` passes with zero errors
2. `bun run lint` passes
3. GET /api/reputation/1 returns JSON with `summary` and `history` fields, all Zod-validated
4. GET /api/reputation/abc returns 400 error
5. /proposals/{id}/reputation page renders without errors
6. /proposals/{id}/reputation page calls chain reader directly (NO self-fetch to /api/reputation)
7. All reputation components have proper aria-label attributes
8. TransactionLink generates correct sepolia.basescan.org URLs using real txHashes (not feedbackHash)
9. API response includes Cache-Control header
10. reputation-schemas.ts exports Zod schemas used by both the API route and the page component
11. on-chain-status-badge.tsx has NO `as const` assertions
12. reputation.ts uses Promise.all for getBlock calls and Promise.allSettled for getLogs calls
</verification>

<success_criteria>
- User can navigate to /proposals/{id}/reputation and see full reputation history with scores, timestamps, and on-chain links
- Each evaluation entry has a clickable link to the Base Sepolia block explorer transaction that resolves to the actual giveFeedback transaction (not a 404)
- Transaction hashes can be copied to clipboard with one click
- Summary card shows average score across all evaluations and total count
- Responsive layout: table on desktop (md+), cards on mobile
- Empty states render for unregistered projects and projects with no evaluations
- All data is read directly from the on-chain ReputationRegistry (no centralized database dependency)
- Page server component calls chain reader functions directly with unstable_cache caching (no self-fetch)
- API route exists for external consumers with Zod validation and Cache-Control headers
- RPC calls are parallelized (Promise.all for getBlock, Promise.allSettled for getLogs)
- No `as const` type assertions in any component
</success_criteria>

<output>
After completion, create `.planning/phases/03-on-chain-reputation/03-01-SUMMARY.md`
</output>
